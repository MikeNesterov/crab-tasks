# Crab Tasks — Task Tracker UI

## Описание
Веб-интерфейс для отображения задач AI-ассистента Краба.
Хостится на GitHub Pages, данные из JSON-файлов.

## Функционал

### Отображение
- 📋 **Cron Jobs** — расписание автоматических задач
- ✅ **TODO** — задачи к выполнению  
- 🔄 **В работе** — текущие задачи
- ✔️ **Выполнено** — архив

### UI Requirements
- Чистый, современный дизайн
- Темная тема (dark mode)
- Адаптивный (mobile-friendly)
- Без фреймворков — чистый HTML/CSS/JS
- Один index.html + стили + скрипт

### Данные
Читает из `data/tasks.json`:
```json
{
  "cronJobs": [
    {"id": "...", "name": "...", "schedule": "...", "enabled": true}
  ],
  "todo": [
    {"id": "...", "title": "...", "priority": "high|medium|low", "created": "..."}
  ],
  "inProgress": [...],
  "done": [...]
}
```

### Стиль
- Эмодзи для категорий
- Карточки для задач
- Цветовая индикация приоритета
- Минималистично, но приятно

## Tech Stack
- HTML5
- CSS3 (no frameworks, custom styles)
- Vanilla JS (ES6+)
- GitHub Pages compatible

## Файловая структура
```
/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── app.js
├── data/
│   └── tasks.json
└── README.md
```
